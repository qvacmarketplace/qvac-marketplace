/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProviderPda } from "../pdas";
import { QVAC_MARKETPLACE_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_PROVIDER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [52, 208, 141, 191, 164, 54, 108, 150],
);

export function getUpdateProviderDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_PROVIDER_DISCRIMINATOR,
  );
}

export type UpdateProviderInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProvider extends string
        ? WritableAccount<TAccountProvider>
        : TAccountProvider,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateProviderInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  taskTypes: number;
};

export type UpdateProviderInstructionDataArgs = {
  name: string;
  taskTypes: number;
};

export function getUpdateProviderInstructionDataEncoder(): Encoder<UpdateProviderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["taskTypes", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_PROVIDER_DISCRIMINATOR }),
  );
}

export function getUpdateProviderInstructionDataDecoder(): Decoder<UpdateProviderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["taskTypes", getU16Decoder()],
  ]);
}

export function getUpdateProviderInstructionDataCodec(): Codec<
  UpdateProviderInstructionDataArgs,
  UpdateProviderInstructionData
> {
  return combineCodec(
    getUpdateProviderInstructionDataEncoder(),
    getUpdateProviderInstructionDataDecoder(),
  );
}

export type UpdateProviderAsyncInput<
  TAccountProvider extends string = string,
  TAccountAuthority extends string = string,
> = {
  /**
   * Existing Provider PDA. `has_one` enforces that the signer matches
   * `provider.authority`. Version check rejects accounts created by a
   * future incompatible build.
   */
  provider?: Address<TAccountProvider>;
  authority: TransactionSigner<TAccountAuthority>;
  name: UpdateProviderInstructionDataArgs["name"];
  taskTypes: UpdateProviderInstructionDataArgs["taskTypes"];
};

export async function getUpdateProviderInstructionAsync<
  TAccountProvider extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: UpdateProviderAsyncInput<TAccountProvider, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateProviderInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QVAC_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.provider.value) {
    accounts.provider.value = await findProviderPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateProviderInstructionDataEncoder().encode(
      args as UpdateProviderInstructionDataArgs,
    ),
    programAddress,
  } as UpdateProviderInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountAuthority
  >);
}

export type UpdateProviderInput<
  TAccountProvider extends string = string,
  TAccountAuthority extends string = string,
> = {
  /**
   * Existing Provider PDA. `has_one` enforces that the signer matches
   * `provider.authority`. Version check rejects accounts created by a
   * future incompatible build.
   */
  provider: Address<TAccountProvider>;
  authority: TransactionSigner<TAccountAuthority>;
  name: UpdateProviderInstructionDataArgs["name"];
  taskTypes: UpdateProviderInstructionDataArgs["taskTypes"];
};

export function getUpdateProviderInstruction<
  TAccountProvider extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: UpdateProviderInput<TAccountProvider, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateProviderInstruction<
  TProgramAddress,
  TAccountProvider,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QVAC_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateProviderInstructionDataEncoder().encode(
      args as UpdateProviderInstructionDataArgs,
    ),
    programAddress,
  } as UpdateProviderInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountAuthority
  >);
}

export type ParsedUpdateProviderInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Existing Provider PDA. `has_one` enforces that the signer matches
     * `provider.authority`. Version check rejects accounts created by a
     * future incompatible build.
     */
    provider: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateProviderInstructionData;
};

export function parseUpdateProviderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateProviderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { provider: getNextAccount(), authority: getNextAccount() },
    data: getUpdateProviderInstructionDataDecoder().decode(instruction.data),
  };
}
