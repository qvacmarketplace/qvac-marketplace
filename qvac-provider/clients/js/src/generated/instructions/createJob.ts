/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findJobPda } from "../pdas";
import { QVAC_MARKETPLACE_PROGRAM_ADDRESS } from "../programs";

export const CREATE_JOB_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  178, 130, 217, 110, 100, 27, 82, 119,
]);

export function getCreateJobDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_JOB_DISCRIMINATOR);
}

export type CreateJobInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountConsumer extends string | AccountMeta<string> = string,
  TAccountInstructions extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountJob extends string ? WritableAccount<TAccountJob> : TAccountJob,
      TAccountProvider extends string
        ? ReadonlyAccount<TAccountProvider>
        : TAccountProvider,
      TAccountConsumer extends string
        ? WritableSignerAccount<TAccountConsumer> &
            AccountSignerMeta<TAccountConsumer>
        : TAccountConsumer,
      TAccountInstructions extends string
        ? ReadonlyAccount<TAccountInstructions>
        : TAccountInstructions,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateJobInstructionData = {
  discriminator: ReadonlyUint8Array;
  requestHash: ReadonlyUint8Array;
  nonce: bigint;
  amount: bigint;
  paymentMint: Address;
  quoteSignature: ReadonlyUint8Array;
  taskType: number;
  validUntil: bigint;
  quoteNonce: ReadonlyUint8Array;
};

export type CreateJobInstructionDataArgs = {
  requestHash: ReadonlyUint8Array;
  nonce: number | bigint;
  amount: number | bigint;
  paymentMint: Address;
  quoteSignature: ReadonlyUint8Array;
  taskType: number;
  validUntil: number | bigint;
  quoteNonce: ReadonlyUint8Array;
};

export function getCreateJobInstructionDataEncoder(): FixedSizeEncoder<CreateJobInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["requestHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["nonce", getU64Encoder()],
      ["amount", getU64Encoder()],
      ["paymentMint", getAddressEncoder()],
      ["quoteSignature", fixEncoderSize(getBytesEncoder(), 64)],
      ["taskType", getU8Encoder()],
      ["validUntil", getI64Encoder()],
      ["quoteNonce", fixEncoderSize(getBytesEncoder(), 16)],
    ]),
    (value) => ({ ...value, discriminator: CREATE_JOB_DISCRIMINATOR }),
  );
}

export function getCreateJobInstructionDataDecoder(): FixedSizeDecoder<CreateJobInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["requestHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["nonce", getU64Decoder()],
    ["amount", getU64Decoder()],
    ["paymentMint", getAddressDecoder()],
    ["quoteSignature", fixDecoderSize(getBytesDecoder(), 64)],
    ["taskType", getU8Decoder()],
    ["validUntil", getI64Decoder()],
    ["quoteNonce", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

export function getCreateJobInstructionDataCodec(): FixedSizeCodec<
  CreateJobInstructionDataArgs,
  CreateJobInstructionData
> {
  return combineCodec(
    getCreateJobInstructionDataEncoder(),
    getCreateJobInstructionDataDecoder(),
  );
}

export type CreateJobAsyncInput<
  TAccountJob extends string = string,
  TAccountProvider extends string = string,
  TAccountConsumer extends string = string,
  TAccountInstructions extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * New Job PDA. Seeded by `(consumer, nonce)` so the same consumer
   * can have multiple concurrent jobs without collision.
   */
  job?: Address<TAccountJob>;
  /**
   * Provider being engaged. The seed constraint re-derives the PDA
   * from `provider.authority` and rejects any account that wasn't
   * genuinely created by `register_provider`.
   */
  provider: Address<TAccountProvider>;
  /** Consumer paying for the job. Funds the escrow and the Job PDA rent. */
  consumer: TransactionSigner<TAccountConsumer>;
  /**
   * Instructions sysvar — used to read the Ed25519Program sibling
   * instruction that carries the provider's signed quote.
   */
  instructions?: Address<TAccountInstructions>;
  systemProgram?: Address<TAccountSystemProgram>;
  requestHash: CreateJobInstructionDataArgs["requestHash"];
  nonce: CreateJobInstructionDataArgs["nonce"];
  amount: CreateJobInstructionDataArgs["amount"];
  paymentMint: CreateJobInstructionDataArgs["paymentMint"];
  quoteSignature: CreateJobInstructionDataArgs["quoteSignature"];
  taskType: CreateJobInstructionDataArgs["taskType"];
  validUntil: CreateJobInstructionDataArgs["validUntil"];
  quoteNonce: CreateJobInstructionDataArgs["quoteNonce"];
};

export async function getCreateJobInstructionAsync<
  TAccountJob extends string,
  TAccountProvider extends string,
  TAccountConsumer extends string,
  TAccountInstructions extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: CreateJobAsyncInput<
    TAccountJob,
    TAccountProvider,
    TAccountConsumer,
    TAccountInstructions,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateJobInstruction<
    TProgramAddress,
    TAccountJob,
    TAccountProvider,
    TAccountConsumer,
    TAccountInstructions,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QVAC_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    job: { value: input.job ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: false },
    consumer: { value: input.consumer ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.job.value) {
    accounts.job.value = await findJobPda({
      consumer: getAddressFromResolvedInstructionAccount(
        "consumer",
        accounts.consumer.value,
      ),
      nonce: getNonNullResolvedInstructionInput("nonce", args.nonce),
    });
  }
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("job", accounts.job),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("consumer", accounts.consumer),
      getAccountMeta("instructions", accounts.instructions),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateJobInstructionDataEncoder().encode(
      args as CreateJobInstructionDataArgs,
    ),
    programAddress,
  } as CreateJobInstruction<
    TProgramAddress,
    TAccountJob,
    TAccountProvider,
    TAccountConsumer,
    TAccountInstructions,
    TAccountSystemProgram
  >);
}

export type CreateJobInput<
  TAccountJob extends string = string,
  TAccountProvider extends string = string,
  TAccountConsumer extends string = string,
  TAccountInstructions extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * New Job PDA. Seeded by `(consumer, nonce)` so the same consumer
   * can have multiple concurrent jobs without collision.
   */
  job: Address<TAccountJob>;
  /**
   * Provider being engaged. The seed constraint re-derives the PDA
   * from `provider.authority` and rejects any account that wasn't
   * genuinely created by `register_provider`.
   */
  provider: Address<TAccountProvider>;
  /** Consumer paying for the job. Funds the escrow and the Job PDA rent. */
  consumer: TransactionSigner<TAccountConsumer>;
  /**
   * Instructions sysvar — used to read the Ed25519Program sibling
   * instruction that carries the provider's signed quote.
   */
  instructions?: Address<TAccountInstructions>;
  systemProgram?: Address<TAccountSystemProgram>;
  requestHash: CreateJobInstructionDataArgs["requestHash"];
  nonce: CreateJobInstructionDataArgs["nonce"];
  amount: CreateJobInstructionDataArgs["amount"];
  paymentMint: CreateJobInstructionDataArgs["paymentMint"];
  quoteSignature: CreateJobInstructionDataArgs["quoteSignature"];
  taskType: CreateJobInstructionDataArgs["taskType"];
  validUntil: CreateJobInstructionDataArgs["validUntil"];
  quoteNonce: CreateJobInstructionDataArgs["quoteNonce"];
};

export function getCreateJobInstruction<
  TAccountJob extends string,
  TAccountProvider extends string,
  TAccountConsumer extends string,
  TAccountInstructions extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: CreateJobInput<
    TAccountJob,
    TAccountProvider,
    TAccountConsumer,
    TAccountInstructions,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateJobInstruction<
  TProgramAddress,
  TAccountJob,
  TAccountProvider,
  TAccountConsumer,
  TAccountInstructions,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QVAC_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    job: { value: input.job ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: false },
    consumer: { value: input.consumer ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("job", accounts.job),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("consumer", accounts.consumer),
      getAccountMeta("instructions", accounts.instructions),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateJobInstructionDataEncoder().encode(
      args as CreateJobInstructionDataArgs,
    ),
    programAddress,
  } as CreateJobInstruction<
    TProgramAddress,
    TAccountJob,
    TAccountProvider,
    TAccountConsumer,
    TAccountInstructions,
    TAccountSystemProgram
  >);
}

export type ParsedCreateJobInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * New Job PDA. Seeded by `(consumer, nonce)` so the same consumer
     * can have multiple concurrent jobs without collision.
     */
    job: TAccountMetas[0];
    /**
     * Provider being engaged. The seed constraint re-derives the PDA
     * from `provider.authority` and rejects any account that wasn't
     * genuinely created by `register_provider`.
     */
    provider: TAccountMetas[1];
    /** Consumer paying for the job. Funds the escrow and the Job PDA rent. */
    consumer: TAccountMetas[2];
    /**
     * Instructions sysvar — used to read the Ed25519Program sibling
     * instruction that carries the provider's signed quote.
     */
    instructions: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateJobInstructionData;
};

export function parseCreateJobInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateJobInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      job: getNextAccount(),
      provider: getNextAccount(),
      consumer: getNextAccount(),
      instructions: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateJobInstructionDataDecoder().decode(instruction.data),
  };
}
