/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { QVAC_MARKETPLACE_PROGRAM_ADDRESS } from "../programs";

export const REFUND_JOB_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  194, 186, 15, 90, 246, 119, 89, 240,
]);

export function getRefundJobDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REFUND_JOB_DISCRIMINATOR);
}

export type RefundJobInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountConsumer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountJob extends string ? WritableAccount<TAccountJob> : TAccountJob,
      TAccountConsumer extends string
        ? WritableSignerAccount<TAccountConsumer> &
            AccountSignerMeta<TAccountConsumer>
        : TAccountConsumer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundJobInstructionData = { discriminator: ReadonlyUint8Array };

export type RefundJobInstructionDataArgs = {};

export function getRefundJobInstructionDataEncoder(): FixedSizeEncoder<RefundJobInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REFUND_JOB_DISCRIMINATOR }),
  );
}

export function getRefundJobInstructionDataDecoder(): FixedSizeDecoder<RefundJobInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundJobInstructionDataCodec(): FixedSizeCodec<
  RefundJobInstructionDataArgs,
  RefundJobInstructionData
> {
  return combineCodec(
    getRefundJobInstructionDataEncoder(),
    getRefundJobInstructionDataDecoder(),
  );
}

export type RefundJobInput<
  TAccountJob extends string = string,
  TAccountConsumer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Job to refund. `has_one = consumer` enforces the signer is the
   * original consumer; only the original consumer can refund.
   * State and timeout checks live in the handler body to provide
   * granular error variants.
   */
  job: Address<TAccountJob>;
  /**
   * Original consumer. Receives both the escrowed `amount` (drained
   * manually below) and the rent-exempt minimum (via Anchor close).
   */
  consumer: TransactionSigner<TAccountConsumer>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRefundJobInstruction<
  TAccountJob extends string,
  TAccountConsumer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: RefundJobInput<TAccountJob, TAccountConsumer, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): RefundJobInstruction<
  TProgramAddress,
  TAccountJob,
  TAccountConsumer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QVAC_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    job: { value: input.job ?? null, isWritable: true },
    consumer: { value: input.consumer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("job", accounts.job),
      getAccountMeta("consumer", accounts.consumer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRefundJobInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundJobInstruction<
    TProgramAddress,
    TAccountJob,
    TAccountConsumer,
    TAccountSystemProgram
  >);
}

export type ParsedRefundJobInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Job to refund. `has_one = consumer` enforces the signer is the
     * original consumer; only the original consumer can refund.
     * State and timeout checks live in the handler body to provide
     * granular error variants.
     */
    job: TAccountMetas[0];
    /**
     * Original consumer. Receives both the escrowed `amount` (drained
     * manually below) and the rent-exempt minimum (via Anchor close).
     */
    consumer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: RefundJobInstructionData;
};

export function parseRefundJobInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRefundJobInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      job: getNextAccount(),
      consumer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRefundJobInstructionDataDecoder().decode(instruction.data),
  };
}
