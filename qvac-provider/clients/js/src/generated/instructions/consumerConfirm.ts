/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { QVAC_MARKETPLACE_PROGRAM_ADDRESS } from "../programs";

export const CONSUMER_CONFIRM_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([209, 98, 123, 63, 85, 202, 94, 236]);

export function getConsumerConfirmDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CONSUMER_CONFIRM_DISCRIMINATOR,
  );
}

export type ConsumerConfirmInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountConsumer extends string | AccountMeta<string> = string,
  TAccountProviderAuthority extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountJob extends string ? WritableAccount<TAccountJob> : TAccountJob,
      TAccountProvider extends string
        ? WritableAccount<TAccountProvider>
        : TAccountProvider,
      TAccountConsumer extends string
        ? WritableAccount<TAccountConsumer>
        : TAccountConsumer,
      TAccountProviderAuthority extends string
        ? WritableAccount<TAccountProviderAuthority>
        : TAccountProviderAuthority,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ConsumerConfirmInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ConsumerConfirmInstructionDataArgs = {};

export function getConsumerConfirmInstructionDataEncoder(): FixedSizeEncoder<ConsumerConfirmInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CONSUMER_CONFIRM_DISCRIMINATOR }),
  );
}

export function getConsumerConfirmInstructionDataDecoder(): FixedSizeDecoder<ConsumerConfirmInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getConsumerConfirmInstructionDataCodec(): FixedSizeCodec<
  ConsumerConfirmInstructionDataArgs,
  ConsumerConfirmInstructionData
> {
  return combineCodec(
    getConsumerConfirmInstructionDataEncoder(),
    getConsumerConfirmInstructionDataDecoder(),
  );
}

export type ConsumerConfirmInput<
  TAccountJob extends string = string,
  TAccountProvider extends string = string,
  TAccountConsumer extends string = string,
  TAccountProviderAuthority extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Job being settled. Closed after settlement; rent → consumer. */
  job: Address<TAccountJob>;
  /**
   * Provider whose counters get incremented (`jobs_completed`, `total_earned`).
   * PDA re-derivation guarantees `provider.authority` matches the seed,
   * and the constraint binds it to the original Job.
   */
  provider: Address<TAccountProvider>;
  /**
   * Rent recipient on close. Must equal `job.consumer`. May or may
   * not be the signer — when the signer is someone else (auto-release),
   * the consumer still gets the rent back.
   */
  consumer: Address<TAccountConsumer>;
  /**
   * Payment recipient. Must equal `job.provider_authority` (snapshotted
   * at `create_job` so a Provider authority change after the fact does
   * not redirect funds).
   */
  providerAuthority: Address<TAccountProviderAuthority>;
  /**
   * Either the consumer (any time) or anyone (after CONFIRM_WINDOW).
   * The window check lives in the handler body.
   */
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getConsumerConfirmInstruction<
  TAccountJob extends string,
  TAccountProvider extends string,
  TAccountConsumer extends string,
  TAccountProviderAuthority extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: ConsumerConfirmInput<
    TAccountJob,
    TAccountProvider,
    TAccountConsumer,
    TAccountProviderAuthority,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ConsumerConfirmInstruction<
  TProgramAddress,
  TAccountJob,
  TAccountProvider,
  TAccountConsumer,
  TAccountProviderAuthority,
  TAccountSigner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QVAC_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    job: { value: input.job ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: true },
    consumer: { value: input.consumer ?? null, isWritable: true },
    providerAuthority: {
      value: input.providerAuthority ?? null,
      isWritable: true,
    },
    signer: { value: input.signer ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("job", accounts.job),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("consumer", accounts.consumer),
      getAccountMeta("providerAuthority", accounts.providerAuthority),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getConsumerConfirmInstructionDataEncoder().encode({}),
    programAddress,
  } as ConsumerConfirmInstruction<
    TProgramAddress,
    TAccountJob,
    TAccountProvider,
    TAccountConsumer,
    TAccountProviderAuthority,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type ParsedConsumerConfirmInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Job being settled. Closed after settlement; rent → consumer. */
    job: TAccountMetas[0];
    /**
     * Provider whose counters get incremented (`jobs_completed`, `total_earned`).
     * PDA re-derivation guarantees `provider.authority` matches the seed,
     * and the constraint binds it to the original Job.
     */
    provider: TAccountMetas[1];
    /**
     * Rent recipient on close. Must equal `job.consumer`. May or may
     * not be the signer — when the signer is someone else (auto-release),
     * the consumer still gets the rent back.
     */
    consumer: TAccountMetas[2];
    /**
     * Payment recipient. Must equal `job.provider_authority` (snapshotted
     * at `create_job` so a Provider authority change after the fact does
     * not redirect funds).
     */
    providerAuthority: TAccountMetas[3];
    /**
     * Either the consumer (any time) or anyone (after CONFIRM_WINDOW).
     * The window check lives in the handler body.
     */
    signer: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: ConsumerConfirmInstructionData;
};

export function parseConsumerConfirmInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedConsumerConfirmInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      job: getNextAccount(),
      provider: getNextAccount(),
      consumer: getNextAccount(),
      providerAuthority: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getConsumerConfirmInstructionDataDecoder().decode(instruction.data),
  };
}
