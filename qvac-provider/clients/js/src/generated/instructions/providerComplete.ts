/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProviderPda } from "../pdas";
import { QVAC_MARKETPLACE_PROGRAM_ADDRESS } from "../programs";

export const PROVIDER_COMPLETE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([98, 191, 214, 128, 186, 30, 8, 120]);

export function getProviderCompleteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROVIDER_COMPLETE_DISCRIMINATOR,
  );
}

export type ProviderCompleteInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountJob extends string ? WritableAccount<TAccountJob> : TAccountJob,
      TAccountProvider extends string
        ? ReadonlyAccount<TAccountProvider>
        : TAccountProvider,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ProviderCompleteInstructionData = {
  discriminator: ReadonlyUint8Array;
  responseHash: ReadonlyUint8Array;
};

export type ProviderCompleteInstructionDataArgs = {
  responseHash: ReadonlyUint8Array;
};

export function getProviderCompleteInstructionDataEncoder(): FixedSizeEncoder<ProviderCompleteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["responseHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: PROVIDER_COMPLETE_DISCRIMINATOR }),
  );
}

export function getProviderCompleteInstructionDataDecoder(): FixedSizeDecoder<ProviderCompleteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["responseHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getProviderCompleteInstructionDataCodec(): FixedSizeCodec<
  ProviderCompleteInstructionDataArgs,
  ProviderCompleteInstructionData
> {
  return combineCodec(
    getProviderCompleteInstructionDataEncoder(),
    getProviderCompleteInstructionDataDecoder(),
  );
}

export type ProviderCompleteAsyncInput<
  TAccountJob extends string = string,
  TAccountProvider extends string = string,
  TAccountAuthority extends string = string,
> = {
  /**
   * Job to mark as ProviderDone. Must be in Pending state and bound
   * to the Provider account passed in. The seed constraint
   * re-derives the Job PDA from its stored consumer + nonce so a
   * caller cannot pass an arbitrary look-alike account.
   */
  job: Address<TAccountJob>;
  /**
   * Provider PDA, re-derived from `authority` so we know the signer
   * is the legitimate authority for THIS Provider record.
   */
  provider?: Address<TAccountProvider>;
  /** Provider authority — must sign and must match `job.provider_authority`. */
  authority: TransactionSigner<TAccountAuthority>;
  responseHash: ProviderCompleteInstructionDataArgs["responseHash"];
};

export async function getProviderCompleteInstructionAsync<
  TAccountJob extends string,
  TAccountProvider extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: ProviderCompleteAsyncInput<
    TAccountJob,
    TAccountProvider,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProviderCompleteInstruction<
    TProgramAddress,
    TAccountJob,
    TAccountProvider,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QVAC_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    job: { value: input.job ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.provider.value) {
    accounts.provider.value = await findProviderPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("job", accounts.job),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getProviderCompleteInstructionDataEncoder().encode(
      args as ProviderCompleteInstructionDataArgs,
    ),
    programAddress,
  } as ProviderCompleteInstruction<
    TProgramAddress,
    TAccountJob,
    TAccountProvider,
    TAccountAuthority
  >);
}

export type ProviderCompleteInput<
  TAccountJob extends string = string,
  TAccountProvider extends string = string,
  TAccountAuthority extends string = string,
> = {
  /**
   * Job to mark as ProviderDone. Must be in Pending state and bound
   * to the Provider account passed in. The seed constraint
   * re-derives the Job PDA from its stored consumer + nonce so a
   * caller cannot pass an arbitrary look-alike account.
   */
  job: Address<TAccountJob>;
  /**
   * Provider PDA, re-derived from `authority` so we know the signer
   * is the legitimate authority for THIS Provider record.
   */
  provider: Address<TAccountProvider>;
  /** Provider authority — must sign and must match `job.provider_authority`. */
  authority: TransactionSigner<TAccountAuthority>;
  responseHash: ProviderCompleteInstructionDataArgs["responseHash"];
};

export function getProviderCompleteInstruction<
  TAccountJob extends string,
  TAccountProvider extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: ProviderCompleteInput<
    TAccountJob,
    TAccountProvider,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): ProviderCompleteInstruction<
  TProgramAddress,
  TAccountJob,
  TAccountProvider,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QVAC_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    job: { value: input.job ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("job", accounts.job),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getProviderCompleteInstructionDataEncoder().encode(
      args as ProviderCompleteInstructionDataArgs,
    ),
    programAddress,
  } as ProviderCompleteInstruction<
    TProgramAddress,
    TAccountJob,
    TAccountProvider,
    TAccountAuthority
  >);
}

export type ParsedProviderCompleteInstruction<
  TProgram extends string = typeof QVAC_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Job to mark as ProviderDone. Must be in Pending state and bound
     * to the Provider account passed in. The seed constraint
     * re-derives the Job PDA from its stored consumer + nonce so a
     * caller cannot pass an arbitrary look-alike account.
     */
    job: TAccountMetas[0];
    /**
     * Provider PDA, re-derived from `authority` so we know the signer
     * is the legitimate authority for THIS Provider record.
     */
    provider: TAccountMetas[1];
    /** Provider authority — must sign and must match `job.provider_authority`. */
    authority: TAccountMetas[2];
  };
  data: ProviderCompleteInstructionData;
};

export function parseProviderCompleteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProviderCompleteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      job: getNextAccount(),
      provider: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getProviderCompleteInstructionDataDecoder().decode(instruction.data),
  };
}
